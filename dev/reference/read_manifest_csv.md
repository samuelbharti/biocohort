# Read a manifest CSV file

Read a manifest CSV file

## Usage

``` r
read_manifest_csv(path, ...)
```

## Arguments

- path:

  Path to a CSV manifest.

- ...:

  Additional arguments passed to readr::read_csv.

## Value

A list with `subject_tbl` and `sample_map`.
