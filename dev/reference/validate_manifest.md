# Validate a manifest table

Validate a manifest table

## Usage

``` r
validate_manifest(manifest)
```

## Arguments

- manifest:

  A data.frame or tibble with subject metadata and assay IDs.

## Value

A list with `subject_tbl` and `sample_map`.
