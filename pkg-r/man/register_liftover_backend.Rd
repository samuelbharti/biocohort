% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liftover.R
\name{register_liftover_backend}
\alias{register_liftover_backend}
\title{Register a liftover backend}
\usage{
register_liftover_backend(name, fn)
}
\arguments{
\item{name}{Character scalar naming the backend.}

\item{fn}{A function with signature \verb{function(intervals, chain, ...)} that
returns a list with two tibbles:
\itemize{
\item \code{mapped}: translated features, including a \code{.feature_id} column linking
each output row to its input row in \code{intervals}.
\item \code{unmapped}: the input rows (carrying \code{.feature_id}) that produced no
output.
}}
}
\value{
Invisibly, the backend name.
}
\description{
Adds a named liftover backend so it can be selected by name in
\code{\link[=liftover_intervals]{liftover_intervals()}}. A backend is a function that performs coordinate
translation for a set of intervals; this pluggable design lets the package
default to an R-native engine while allowing external tools (e.g. CrossMap)
to be swapped in.
}
\details{
\code{intervals} passed to a backend is guaranteed to have columns \code{seqnames},
\code{start}, \code{end}, an optional \code{strand}, and a \code{.feature_id} integer key added
by \code{\link[=liftover_intervals]{liftover_intervals()}}.
}
\examples{
# A backend that maps every interval onto itself, then use it by name.
passthrough <- function(intervals, chain, ...) {
  list(
    mapped = tibble::tibble(
      .feature_id = intervals$.feature_id,
      seqnames = intervals$seqnames,
      start = intervals$start,
      end = intervals$end,
      strand = "*"
    ),
    unmapped = intervals[0, , drop = FALSE]
  )
}
register_liftover_backend("passthrough", passthrough)
"passthrough" \%in\% liftover_backends()

ints <- data.frame(seqnames = "chr1", start = 100, end = 200)
liftover_intervals(ints, chain = "none", to = "human", backend = "passthrough")

}
\seealso{
\code{\link[=liftover_backends]{liftover_backends()}}, \code{\link[=liftover_intervals]{liftover_intervals()}},
\code{\link[=liftover_rtracklayer]{liftover_rtracklayer()}}, \code{\link[=liftover_crossmap]{liftover_crossmap()}}
}
