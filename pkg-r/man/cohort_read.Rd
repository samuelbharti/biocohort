% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{cohort_read}
\alias{cohort_read}
\title{Read a cohort saved with cohort_save()}
\usage{
cohort_read(path)
}
\arguments{
\item{path}{Path to a file written by \code{\link[=cohort_save]{cohort_save()}}.}
}
\value{
The \link{Cohort} object.
}
\description{
Reads the RDS file, checks it is a cohort file, and re-validates the
cohort before returning it.
}
\examples{
data(example_cohort)
path <- tempfile(fileext = ".rds")
cohort_save(example_cohort, path)

restored <- cohort_read(path)
identical(subjects(restored), subjects(example_cohort))
unlink(path)

}
\seealso{
\code{\link[=cohort_save]{cohort_save()}}
}
