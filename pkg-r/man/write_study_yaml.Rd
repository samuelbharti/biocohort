% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_yaml.R
\name{write_study_yaml}
\alias{write_study_yaml}
\title{Write a cohort as a study YAML file}
\usage{
write_study_yaml(cohort, path, manifest = "manifest.csv")
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{path}{Output path for the study YAML file.}

\item{manifest}{Path for the manifest file, resolved relative to \code{path}'s
directory unless absolute. Default \code{"manifest.csv"}.}
}
\value{
\code{path}, invisibly.
}
\description{
The inverse of \code{\link[=read_study_yaml]{read_study_yaml()}}: writes the cohort's study metadata,
its manifest, its paths, and its registered analysis specs to a study
YAML file and a manifest file alongside it.
}
\details{
A cohort has no stored corrections file, so a \verb{corrections:} key is never
written; the manifest written out already reflects any correction that
was applied before the cohort was built.
}
\examples{
data(example_cohort)
dir <- tempfile()
dir.create(dir)
write_study_yaml(example_cohort, file.path(dir, "study.yaml"))
cat(readLines(file.path(dir, "study.yaml")), sep = "\n")

}
\seealso{
\code{\link[=read_study_yaml]{read_study_yaml()}}, \code{\link[=write_manifest]{write_manifest()}}
}
