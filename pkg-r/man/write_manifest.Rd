% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{write_manifest}
\alias{write_manifest}
\title{Write a manifest or a cohort's tables to a delimited file}
\usage{
write_manifest(x, path, delim = NULL)
}
\arguments{
\item{x}{A \link{Cohort} object, or the list returned by \code{\link[=validate_manifest]{validate_manifest()}}
or \code{\link[=read_manifest]{read_manifest()}} (anything with \code{subject_tbl} and \code{sample_map}).}

\item{path}{Output file path. The delimiter is chosen from the extension
unless \code{delim} is given.}

\item{delim}{Optional character scalar overriding delimiter detection.}
}
\value{
The written manifest tibble, invisibly.
}
\description{
Joins a cohort's (or a \code{\link[=validate_manifest]{validate_manifest()}} result's) \code{sample_map} and
\code{subject_tbl} back into one long-format manifest and writes it to a CSV,
TSV, or other delimited file.
}
\details{
Columns are ordered \code{subject_id}, then the other subject-level columns,
then the sample-level columns (\code{assay}, \code{sample_id}, \code{role}, and any
extra ones). A missing value is written as an empty field.
}
\examples{
manifest <- data.frame(
  subject_id = "R1",
  species = "rat",
  assay = "wes",
  sample_id = "T1",
  role = "tumor",
  stringsAsFactors = FALSE
)
parsed <- validate_manifest(manifest)

out <- tempfile(fileext = ".csv")
write_manifest(parsed, out)
readLines(out)

}
\seealso{
\code{\link[=read_manifest]{read_manifest()}}, \code{\link[=validate_manifest]{validate_manifest()}}, \code{\link[=cohort_save]{cohort_save()}}
}
