% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{completeness}
\alias{completeness}
\title{Per-assay sample counts for a cohort}
\usage{
completeness(cohort, wide = FALSE)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{wide}{Logical. When \code{FALSE} (default), returns one row per
\code{subject_id} x \code{assay} with an \code{n_samples} count. When \code{TRUE}, pivots to
one row per subject and one column per assay, with \code{0} where a subject
has no sample for that assay.}
}
\value{
A tibble. See \code{wide} above for its shape.
}
\description{
Summarizes how many samples each subject has for each assay. This is the
table most studies ask for first: which subjects are missing which assay.
}
\details{
The wide form always has one row per subject in \code{cohort@subject_tbl}, even
a subject with zero samples for every assay, and one column per assay
that appears anywhere in \code{cohort@sample_map}.
}
\examples{
data(example_cohort)
completeness(example_cohort)
completeness(example_cohort, wide = TRUE)

}
\seealso{
\code{\link[=subjects]{subjects()}}, \code{\link[=samples]{samples()}}, \code{\link[=validate_manifest]{validate_manifest()}}
}
