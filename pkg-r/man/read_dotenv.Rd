% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{read_dotenv}
\alias{read_dotenv}
\title{Read a dotenv file into the environment}
\usage{
read_dotenv(path = NULL, overwrite = FALSE)
}
\arguments{
\item{path}{Path to the file. Defaults to \code{.env} in \code{\link[=project_root]{project_root()}}.}

\item{overwrite}{Replace variables that are already set? Defaults to
\code{FALSE}.}
}
\value{
A named character vector of every value read from the file,
invisibly. Keys that were skipped because they were already set are
included.
}
\description{
Reads \code{KEY=value} lines from a dotenv file and sets them with
\code{\link[=Sys.setenv]{Sys.setenv()}}. Blank lines and lines that start with \verb{#} are skipped.
Whitespace around keys and values is trimmed. One pair of matching single
or double quotes around a value is removed. A key that is already set in
the environment is left alone unless \code{overwrite = TRUE}. When a key repeats
in the file, the last line wins. Values are never printed.
}
\examples{
env_file <- tempfile(fileext = ".env")
writeLines(
  c(
    "# analysis settings",
    "BIOCOHORT_EXAMPLE_THREADS = 4",
    "BIOCOHORT_EXAMPLE_LABEL = 'batch one'"
  ),
  env_file
)

vars <- read_dotenv(env_file)
names(vars)
Sys.getenv("BIOCOHORT_EXAMPLE_LABEL")

Sys.unsetenv(names(vars))
unlink(env_file)
}
