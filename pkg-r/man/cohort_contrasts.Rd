% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groups.R
\name{cohort_contrasts}
\alias{cohort_contrasts}
\title{Every pairwise contrast between a cohort's groups}
\usage{
cohort_contrasts(x, by = NULL)
}
\arguments{
\item{x}{Either a \link{Cohort} (then \code{by} is required, and \code{\link[=cohort_groups]{cohort_groups()}}
runs internally) or the tibble \code{\link[=cohort_groups]{cohort_groups()}} already returned (then
\code{by} must not be given).}

\item{by}{Character vector of \code{subject_tbl} columns to group by. Only
used when \code{x} is a Cohort.}
}
\value{
A tibble with one row per pair: \code{group_a}, \code{group_b} (the two
groups' labels), \code{subject_ids_a}, \code{subject_ids_b} (list-columns of
subject ids), and \code{n_a}, \code{n_b} (their sizes).
}
\description{
Enumerates every pairwise combination of the groups in \code{x}, so a caller
can turn each pair into a comparison (for example, filtering a cohort down
to one side and diffing an analysis table against the other).
}
\details{
This is deliberately minimal: it does not repeat the raw \code{by} values on
each row. Join back to \code{\link[=cohort_groups]{cohort_groups()}}'s output on \code{group_label} for
those.
}
\examples{
data(example_cohort)
contrasts <- cohort_contrasts(example_cohort, by = "species")
contrasts

cohort_filter(example_cohort, subject_ids = contrasts$subject_ids_a[[1]])

}
\seealso{
\code{\link[=cohort_groups]{cohort_groups()}}, \code{\link[=cohort_filter]{cohort_filter()}}
}
