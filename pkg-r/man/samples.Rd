% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{samples}
\alias{samples}
\title{Read the sample map of a cohort}
\usage{
samples(cohort, assay = NULL, role = NULL, with_subjects = FALSE)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{assay}{Optional character vector. Keep only these assays.}

\item{role}{Optional character vector. Keep only these roles.}

\item{with_subjects}{Logical. When \code{TRUE}, left-joins the subject table on
\code{subject_id}, so subject-level columns (species, genotype, ...) sit
alongside each sample row. Default \code{FALSE}.}
}
\value{
A tibble with the sample map, filtered and optionally joined.
}
\description{
A thin, named accessor for \code{cohort@sample_map}, with optional filters and
an optional join to the subject table.
}
\examples{
data(example_cohort)
samples(example_cohort, assay = "wes")
samples(example_cohort, role = "tumor", with_subjects = TRUE)

}
\seealso{
\code{\link[=subjects]{subjects()}}, \code{\link[=completeness]{completeness()}}, \code{\link[=sample_pairs]{sample_pairs()}}
}
