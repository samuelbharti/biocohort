% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translation-result.R
\name{TranslationResult}
\alias{TranslationResult}
\title{Result of a cross-species or cross-assembly translation}
\usage{
TranslationResult(
  mapped = NULL,
  unmapped = NULL,
  from = NA_character_,
  to = NA_character_,
  backend = NA_character_,
  stats = list()
)
}
\arguments{
\item{mapped}{A tibble of successfully translated features. Includes a
\code{.feature_id} column linking each output row back to its input row; one
input may yield multiple output rows (multi-mapping).}

\item{unmapped}{A tibble of input features that produced no output.}

\item{from}{Character scalar naming the source species/assembly. Optional.}

\item{to}{Character scalar naming the target species/assembly. Optional.}

\item{backend}{Character scalar naming the translation backend used. Optional.}

\item{stats}{Named list of summary counts (\code{n_input}, \code{n_mapped},
\code{n_unmapped}, \code{n_multi}).}
}
\description{
An S7 class holding the outcome of translating coordinate features or
gene-level features from one species or assembly to another. It keeps the
successfully translated features, the features that failed to map, and
provenance so that translation is never lossy \emph{silently}.
}
\details{
Construct these via \code{\link[=liftover_intervals]{liftover_intervals()}} or \code{\link[=orthologize]{orthologize()}} rather than
directly. Access the pieces with \code{result@mapped}, \code{result@unmapped}, and
\code{\link[=translation_stats]{translation_stats()}}.
}
\seealso{
\code{\link[=liftover_intervals]{liftover_intervals()}}, \code{\link[=orthologize]{orthologize()}}, \code{\link[=translation_stats]{translation_stats()}}
}
