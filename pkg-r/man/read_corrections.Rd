% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corrections.R
\name{read_corrections}
\alias{read_corrections}
\title{Read a corrections table from a file}
\usage{
read_corrections(path)
}
\arguments{
\item{path}{Path to a \code{.csv} or \code{.tsv} file.}
}
\value{
A tibble with character columns \code{level}, \code{id}, \code{column}, \code{value},
and \code{reason}, plus any other column in the file.
}
\description{
Reads a CSV or TSV file, chosen by extension, with every column as
character, and checks that the columns \code{\link[=apply_corrections]{apply_corrections()}} needs are
present. An empty value or \code{NA} in the file becomes \code{NA}.
}
\examples{
path <- tempfile(fileext = ".csv")
writeLines(
  c(
    "level,id,column,value,reason",
    "subject,R1,genotype,KO,genotyping rerun",
    "sample,R2_T,fastq,r2_tumor.fq.gz,vendor renamed the file"
  ),
  path
)

read_corrections(path)

unlink(path)
}
\seealso{
\code{\link[=apply_corrections]{apply_corrections()}}
}
