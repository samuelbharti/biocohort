% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groups.R
\name{cohort_groups}
\alias{cohort_groups}
\title{Group a cohort's subjects by one or more columns}
\usage{
cohort_groups(cohort, by)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{by}{Character vector of one or more \code{subject_tbl} column names to
group by. Required, no default.}
}
\value{
A tibble with the \code{by} columns, \code{group_label} (the \code{by} values
pasted together with \code{"/"}), \code{n} (the number of subjects), and
\code{subject_ids} (a list-column of sorted, unique subject ids).
}
\description{
Groups \code{cohort@subject_tbl} by the given columns and returns one row per
combination that actually occurs, with the matching subject ids.
}
\details{
Only combinations that occur in \code{subject_tbl} appear; this never invents
a row for a combination of values that no subject has. An \code{NA} in a \code{by}
column forms its own group rather than being dropped.
}
\examples{
data(example_cohort)
cohort_groups(example_cohort, by = "species")

}
\seealso{
\code{\link[=cohort_contrasts]{cohort_contrasts()}}, \code{\link[=subjects]{subjects()}}
}
