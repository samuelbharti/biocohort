% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{subject}
\alias{subject}
\title{Build one Subject from a cohort}
\usage{
subject(cohort, id)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{id}{Character scalar with the \code{subject_id} to look up.}
}
\value{
A \link{Subject} built from the matching row.
}
\description{
Reads the row of \code{cohort@subject_tbl} whose \code{subject_id} equals \code{id} and
returns it as a \link{Subject} object. A cohort stores its subjects as a table.
Use this function when one subject is needed as an object.
}
\details{
The Subject fields are read from the columns \code{subject_id}, \code{species},
\code{sex}, \code{strain}, \code{genotype}, \code{cohort}, \code{timepoint}, and \code{notes}. Values are
coerced to character and missing values stay missing. A column that is
absent from \code{subject_tbl} gives \code{NA}. Other columns are ignored.

An unknown \code{id} is an error. The error lists up to five ids that the
cohort does have.
}
\examples{
data(example_cohort)
rat1 <- subject(example_cohort, "RAT001")
rat1@species
rat1@sex

}
\seealso{
\link{Subject}, \code{\link[=subject_new]{subject_new()}}, \code{\link[=cohort_new]{cohort_new()}}
}
