% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{Study}
\alias{Study}
\title{S7 Study class}
\usage{
Study(
  study_id = character(0),
  title = character(0),
  description = NA_character_,
  hypotheses = character(0),
  aims = character(0),
  assays = character(0),
  genome_builds = list(),
  created_at = Sys.time(),
  tags = character(0)
)
}
\arguments{
\item{study_id}{Character scalar for study identifier. Unique within a project.}

\item{title}{Character scalar for study name/title.}

\item{description}{Character scalar for longer description of study purpose,
design, or protocols. Optional.}

\item{hypotheses}{Character vector of research hypotheses. Accepts multiple
hypotheses. Optional.}

\item{aims}{Character vector of specific research aims. Accepts multiple aims.
Optional.}

\item{assays}{Character vector of assay types used (e.g., "wes", "scrna").
Optional.}

\item{genome_builds}{Named list mapping species to genome build versions,
e.g. \code{list(rat = "rn7", mouse = "mm39", human = "hg38")}. Any species
name and any build string are accepted. Optional.}

\item{created_at}{POSIXct timestamp for creation. Defaults to the time the
object is built.}

\item{tags}{Character vector of arbitrary tags for categorization. Optional.}
}
\value{
A \code{Study} object with the given properties.
}
\description{
An immutable S7 class for storing project-level metadata for a study.
Study objects provide high-level context and configuration for cohorts
and analyses, for any organism and any omics assay.
}
\details{
Use \code{\link[=study_new]{study_new()}} to construct Study objects with immediate validation.
Construction also validates \code{study_id} and \code{title} directly, so building a
\code{Study} any other way still enforces the two required fields.

Access properties via the \code{@} operator:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{study@study_id
study@title
study@description
study@hypotheses
study@aims
study@assays
study@genome_builds
study@created_at
study@tags
}\if{html}{\out{</div>}}
}
\examples{
# The raw constructor; study_new() is the usual way in.
study <- Study(study_id = "PILOT", title = "Pilot study")
study@study_id
study@assays # empty until set

}
\seealso{
\code{\link[=study_new]{study_new()}} for object construction,
\link{Cohort} for combining studies with subject data
}
