% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{cohort_qc}
\alias{cohort_qc}
\title{Flag or drop subjects or samples for quality control}
\usage{
cohort_qc(cohort, ids, scope, action, reason)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{ids}{Character vector of ids to act on. At least one, no \code{NA}.
Duplicates are removed. Their meaning depends on \code{scope}.}

\item{scope}{One of \code{"sample"} or \code{"subject"}: whether \code{ids} are sample
ids (matched against \code{cohort@sample_map$sample_id}) or subject ids
(matched against \code{cohort@subject_tbl$subject_id}). No default.}

\item{action}{One of \code{"flag"} or \code{"drop"}. \code{"flag"} sets \code{qc_status}/
\code{qc_reason} on the matching rows and keeps them. \code{"drop"} removes the
matching rows entirely. No default.}

\item{reason}{A single, non-empty string explaining the decision.}
}
\value{
A new \link{Cohort}.
}
\description{
Records a QC decision against a cohort: either annotate matching rows
with a status and reason (\code{action = "flag"}), or remove them
(\code{action = "drop"}). Every call is recorded in the cohort's QC log (see
\code{\link[=qc_log]{qc_log()}}), which is not cleared by \code{\link[=cohort_filter]{cohort_filter()}}, so the record of
why something was dropped survives later structural changes.
}
\details{
An id in \code{ids} that does not exist for the given \code{scope} is an error; it
is never silently ignored.

\code{action = "flag"}, \code{scope = "sample"} sets \code{qc_status}/\code{qc_reason} on
\code{sample_map}, creating the columns if they are absent. \code{scope = "subject"}
sets the same two column names on \code{subject_tbl} instead; these are a
separate pair of columns from the sample-level ones, and both can be set
on the same cohort for different reasons. Flagging an id that already has
a \code{qc_reason} appends the new reason rather than replacing it.

\code{action = "drop"} delegates to \code{\link[=cohort_filter]{cohort_filter()}}: \code{scope = "sample"} uses
its \code{drop_sample_ids} argument, with \code{drop_empty = FALSE} so a subject
left with no samples is not also removed; \code{scope = "subject"} uses its
\code{subject_ids} argument to keep every other subject. Either way, the
resulting cache reset is the same one a direct \code{\link[=cohort_filter]{cohort_filter()}} call
would produce.
}
\examples{
data(example_cohort)

# Flag one sample, keeping it
bad_id <- samples(example_cohort)$sample_id[[1]]
flagged <- cohort_qc(
  example_cohort, bad_id,
  scope = "sample", action = "flag", reason = "failed QC review"
)
samples(flagged)
qc_log(flagged)

# Drop the same sample instead
dropped <- cohort_qc(
  example_cohort, bad_id,
  scope = "sample", action = "drop", reason = "failed QC review"
)
nrow(samples(dropped)) < nrow(samples(example_cohort))

}
\seealso{
\code{\link[=qc_log]{qc_log()}}, \code{\link[=cohort_filter]{cohort_filter()}}
}
