% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridges.R
\name{join_metadata}
\alias{join_metadata}
\title{Add cohort metadata to an analysis object}
\usage{
join_metadata(object, cohort, assay = NULL, by = "sample_id", col = NULL)
}
\arguments{
\item{object}{A data.frame, a \code{SummarizedExperiment}, or a \code{Seurat}
object.}

\item{cohort}{A \link{Cohort} object.}

\item{assay}{Optional character scalar restricting the join to one
assay's samples. Defaults to \code{NULL}, which matches \code{by} against every
sample in the cohort; this is usually enough, since \code{sample_id} is
unique across the whole cohort unless the manifest allowed duplicates.}

\item{by}{Character scalar naming the sample id column to join on.
Default \code{"sample_id"}. For a data.frame, this must be a column of
\code{object}. For a \code{SummarizedExperiment} or a \code{Seurat} object, \code{by} names
the column of \code{samples(cohort, with_subjects = TRUE)} to match against
(see \code{col}).}

\item{col}{For a \code{SummarizedExperiment}, an optional column of its
\code{colData} holding sample ids; defaults to \code{colnames(object)}. For a
\code{Seurat} object, an optional column of its \code{meta.data} holding sample
ids; defaults to \code{"orig.ident"}.}
}
\value{
\code{object}, with the cohort's metadata columns added: joined
columns for a data.frame, added \code{colData} columns for a
\code{SummarizedExperiment}, added \code{meta.data} columns for a \code{Seurat} object.
}
\description{
Joins a cohort's sample and subject metadata onto a data.frame, a
\code{SummarizedExperiment}, or a \code{Seurat} object, matched by sample id.
}
\details{
A \code{SummarizedExperiment} or \code{Seurat} column that does not match any
sample in the cohort is an error, naming the unmatched ids. A data.frame
join is a plain left join, so it keeps every row of \code{object} and leaves
an unmatched row's new columns as \code{NA}.
}
\examples{
data(example_cohort)
expr <- data.frame(
  sample_id = example_cohort@sample_map$sample_id[1:2],
  value = c(1, 2)
)
join_metadata(expr, example_cohort)

}
\seealso{
\code{\link[=as_coldata]{as_coldata()}}, \code{\link[=samples]{samples()}}
}
