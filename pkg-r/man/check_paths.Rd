% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_paths.R
\name{check_paths}
\alias{check_paths}
\title{Check that a cohort's file paths exist on disk}
\usage{
check_paths(cohort, cols = NULL)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{cols}{Optional character vector of \code{sample_map} column names to
check as paths, overriding the default set of recognized path columns
(\code{fastq_1}, \code{fastq_2}, \code{bam}, \code{cram}, \code{vcf}, \code{matrix_dir}, \code{h5}).}
}
\value{
A tibble with one row per checked path and columns \code{source}
(\code{"sample_map"} or \code{"paths"}), \code{key} (\code{sample_id}, or the name in
\code{cohort@paths}), \code{column} (the \code{sample_map} column, \code{NA} for a
\code{cohort@paths} entry), \code{path}, and \code{exists}. \code{exists} is \code{NA} for a
missing (\code{NA}) path, so an unset path is not read as a broken one.
}
\description{
Tests every path a cohort references: the file-like columns of its sample
map (\code{fastq_1}, \code{bam}, and similar), and every entry of \code{cohort@paths}.
Never errors; a summary is reported when something is missing.
}
\examples{
data(example_cohort)
check_paths(example_cohort)

}
\seealso{
\code{\link[=sample_sheet]{sample_sheet()}}, \code{\link[=samples]{samples()}}
}
