% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{manifest_from_wide}
\alias{manifest_from_wide}
\title{Reshape a wide sample table into a long-format manifest}
\usage{
manifest_from_wide(x, id_cols, subject_id = "subject_id")
}
\arguments{
\item{x}{A data.frame or tibble, one row per subject.}

\item{id_cols}{A data.frame or tibble describing the columns of \code{x} that
hold sample ids, with columns:
\itemize{
\item \code{column}: name of a column in \code{x}.
\item \code{assay}: the assay that column's ids belong to.
\item \code{role}: optional; the role of that column's samples. Defaults to \code{NA}
for every row when absent.
}}

\item{subject_id}{Character scalar naming the subject id column in \code{x}.
Default \code{"subject_id"}.}
}
\value{
A long-format tibble: one row per non-missing sample id in any
\code{id_cols$column}, with \code{subject_id}, \code{assay}, \code{sample_id}, \code{role}, and
every column of \code{x} that is not in \code{id_cols$column}. Pass it to
\code{\link[=validate_manifest]{validate_manifest()}} next.
}
\description{
Many sample sheets start wide: one row per subject, with one column per
assay-and-role combination (e.g. \code{wes_tumor_id}, \code{wes_normal_id},
\code{scrna_id}). This turns such a table into the long format
\code{\link[=validate_manifest]{validate_manifest()}} expects, one row per non-missing sample id.
}
\details{
A blank or \code{NA} value in an id column contributes no row, so a subject
missing one assay is not stamped with an empty sample id.
}
\examples{
wide <- data.frame(
  subject_id = c("R1", "R2"),
  species = "rat",
  wes_tumor_id = c("WES_T1", "WES_T2"),
  wes_normal_id = c("WES_N1", NA),
  scrna_id = c("SC_1", "SC_2")
)
id_cols <- data.frame(
  column = c("wes_tumor_id", "wes_normal_id", "scrna_id"),
  assay = c("wes", "wes", "scrna"),
  role = c("tumor", "normal", NA)
)

long <- manifest_from_wide(wide, id_cols)
long
validate_manifest(long)

}
\seealso{
\code{\link[=read_manifest]{read_manifest()}}, \code{\link[=validate_manifest]{validate_manifest()}}
}
