% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_sheet.R
\name{sample_sheet}
\alias{sample_sheet}
\title{Write a pipeline sample sheet from a cohort}
\usage{
sample_sheet(
  cohort,
  template = "nf-core/rnaseq",
  assay = NULL,
  path = NULL,
  ...
)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{template}{One of:
\itemize{
\item A character scalar naming a built-in template (see
\code{\link[=sample_sheet_templates]{sample_sheet_templates()}}).
\item A named character vector mapping an output column name to a column of
\code{samples(cohort, with_subjects = TRUE)}, e.g.
\code{c(sample = "sample_id", path = "fastq_1")}.
\item A function \verb{function(joined, ...)} returning a data.frame, where
\code{joined} is \code{samples(cohort, assay = assay, with_subjects = TRUE)}.
}

Default \code{"nf-core/rnaseq"}.}

\item{assay}{Character scalar naming the assay to include. Required when
the cohort has more than one assay; optional when it has exactly one.}

\item{path}{Optional output file path. When given, the sheet is written
there as CSV and returned invisibly.}

\item{...}{Passed to a function \code{template}. Ignored for a built-in or a
named-vector template.}
}
\value{
A tibble with one row per sample. Invisible when \code{path} is given.
}
\description{
Builds the sample sheet a pipeline expects, from a cohort's sample map and
subject table. A few common shapes ship with the package (see
\code{\link[=sample_sheet_templates]{sample_sheet_templates()}}); pass a custom mapping or a function for
anything else.
}
\details{
The built-in templates are:
\itemize{
\item \code{"nf-core/rnaseq"}: \code{sample}, \code{fastq_1}, \code{fastq_2}, \code{strandedness}
(\code{"auto"} when the manifest has no \code{strandedness} column).
\item \code{"nf-core/rnavar"}: \code{sample}, \code{fastq_1}, \code{fastq_2}.
\item \code{"nf-core/atacseq"}: \code{sample}, \code{fastq_1}, \code{fastq_2}, \code{replicate}
(\code{1} when the manifest has no \code{replicate} column).
\item \code{"nf-core/sarek"}: \code{patient}, \code{sex} (\code{"XX"}/\code{"XY"}, from a \code{sex} column
of \code{"F"}/\code{"M"}), \code{status} (\code{1} for a \code{"tumor"} or \code{"resistant"} role,
\code{0} otherwise), \code{sample}, \code{lane} (\code{1} when absent), \code{fastq_1},
\code{fastq_2}.
}

Every template needs \code{fastq_1} in the sample map (and \code{fastq_2} where the
template writes it); declare it with \code{validate_manifest(sample_cols = )}
or \code{read_manifest(sample_cols = )} if your manifest names it differently.
}
\examples{
manifest <- data.frame(
  subject_id = c("R1", "R1"),
  species = "rat",
  sex = "F",
  assay = "wes",
  sample_id = c("T1", "N1"),
  role = c("tumor", "normal"),
  fastq_1 = c("t1_R1.fq.gz", "n1_R1.fq.gz"),
  fastq_2 = c("t1_R2.fq.gz", "n1_R2.fq.gz"),
  stringsAsFactors = FALSE
)
parsed <- validate_manifest(manifest)
cohort <- cohort_new(parsed$subject_tbl, parsed$sample_map)

sample_sheet(cohort, template = "nf-core/sarek")

# A custom mapping
sample_sheet(cohort, template = c(sample = "sample_id", read1 = "fastq_1"))

}
\seealso{
\code{\link[=sample_sheet_templates]{sample_sheet_templates()}}, \code{\link[=samples]{samples()}}, \code{\link[=check_paths]{check_paths()}}
}
