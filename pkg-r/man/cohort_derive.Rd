% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive.R
\name{cohort_derive}
\alias{cohort_derive}
\title{Derive a column from cutoffs on an existing numeric column}
\usage{
cohort_derive(cohort, name, from, cutoffs, level)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{name}{Character scalar naming the new column.}

\item{from}{Character scalar naming an existing numeric (or
numeric-coercible) column to bin.}

\item{cutoffs}{A named numeric vector. Sorted internally, so the order
given does not matter. See Details for how names become bins.}

\item{level}{One of \code{"subject"} or \code{"sample"}: whether \code{from} and \code{name}
act on \code{subject_tbl} or \code{sample_map}. No default.}
}
\value{
A new \link{Cohort}.
}
\description{
Bins an existing numeric column into named groups at one or more cutoff
values, and writes the result as a new column, so a rule like "early
onset is 120 days or under" is a value you pass in, not code you write.
}
\details{
Sort \code{cutoffs} by value. Bin \code{i} is everything up to and including
\code{cutoffs[i]}, labeled \code{names(cutoffs)[i]}. The bin above the highest
cutoff is \code{NA}, unless the highest cutoff is itself \code{Inf} with its own
name, in which case that bin is fully covered.

So \code{cutoffs = c(early_onset = 120)} gives two groups: \code{"early_onset"} for
values at or under 120, and \code{NA} above it, a legitimate pattern when only
the named group matters. To cover every value, name the top bin too:
\code{cutoffs = c(early_onset = 120, late_onset = Inf)}. More than one real
cutoff works the same way, at no extra cost.

A value in \code{from} that is not already missing but fails to parse as
numeric is an error naming the offending ids, never a silent \code{NA}. A
value that was already missing stays \code{NA} in the derived column.

Re-deriving with the same \code{name} overwrites the column, the same way
\code{\link[=analysis_register]{analysis_register()}} replaces a spec of the same name. Every call is
recorded in \code{\link[=derive_log]{derive_log()}}, which, like \code{\link[=qc_log]{qc_log()}}, is not cleared by
\code{\link[=cohort_filter]{cohort_filter()}}.
}
\examples{
manifest <- data.frame(
  subject_id = c("S1", "S2", "S3"),
  species = "rat",
  onset_days = c(90, 150, 200),
  assay = "wes", sample_id = c("a", "b", "c"), role = "tumor"
)
parsed <- validate_manifest(manifest)
cohort <- cohort_new(parsed$subject_tbl, parsed$sample_map)

# Only the named group matters; everything above 120 is NA.
only_named <- cohort_derive(
  cohort, "early_only",
  from = "onset_days", cutoffs = c(early_onset = 120), level = "subject"
)
subjects(only_named)

# Fully covering two groups with an Inf-capped top cutoff.
covered <- cohort_derive(
  cohort, "onset_group",
  from = "onset_days", cutoffs = c(early = 120, late = Inf),
  level = "subject"
)
subjects(covered)
derive_log(covered)

}
\seealso{
\code{\link[=derive_log]{derive_log()}}, \code{\link[=cohort_groups]{cohort_groups()}}
}
