% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{example_cohort}
\alias{example_cohort}
\title{Example Cohort Dataset}
\format{
A Cohort object (S7 class) with the following structure:
\itemize{
\item study: A Study object with metadata for a cross-species genomics project
\item subject_tbl (tibble): 4 subjects (2 rat, 2 mouse) with species, sex,
strain, genotype, cohort, timepoint
\item sample_map (tibble): Long-format map (subject_id, assay, sample_id,
role, fastq_1, fastq_2) covering WES tumor/normal and snRNA-seq
samples. fastq_1/fastq_2 show that an extra sample-level column
survives validate_manifest() alongside the four canonical ones.
\item paths (list): Empty, ready for file paths
\item analyses (list): Empty, ready for analysis results
}
}
\usage{
example_cohort
}
\description{
A small Cohort with two rat and two mouse subjects. Use it to explore the
data model, to try the API, or as a template for a cohort built from real
data.
}
\details{
The cohort shows:
\itemize{
\item Two species in one subject table
\item Two assays per subject in one long-format sample map
\item A Study object for project context
}

Subjects are stored as rows of \code{subject_tbl}. Use \code{\link[=subject]{subject()}} to read one
of them as a Subject object.
}
\examples{
# Load the example cohort
data(example_cohort)

# View the study metadata
example_cohort@study

# Read one subject as a Subject object
rat1 <- subject(example_cohort, "RAT001")
rat1@species
rat1@sex

# List all subject ids
example_cohort@subject_tbl$subject_id

# View all subjects with metadata
example_cohort@subject_tbl

# View the sample map
example_cohort@sample_map

# Count subjects by species
table(example_cohort@subject_tbl$species)

}
\seealso{
\code{\link[=cohort_new]{cohort_new()}} for creating Cohort objects,
\code{\link[=subject]{subject()}} for reading one subject,
\code{\link[=validate_manifest]{validate_manifest()}} for preparing manifest data,
\code{\link[=read_manifest_csv]{read_manifest_csv()}} for loading manifest from CSV file
}
\keyword{datasets}
