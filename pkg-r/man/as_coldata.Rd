% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridges.R
\name{as_coldata}
\alias{as_coldata}
\title{Build sample metadata for a count matrix}
\usage{
as_coldata(cohort, assay, samples = NULL, rownames = "sample_id", ref = NULL)
}
\arguments{
\item{cohort}{A \link{Cohort} object.}

\item{assay}{Character scalar naming the assay to include.}

\item{samples}{Optional character vector of sample ids, in the order they
should appear (matching, for example, the column order of a count
matrix). Every id must be one of the cohort's samples for \code{assay};
otherwise this errors and lists the ones it could not find.}

\item{rownames}{Character scalar naming the column to use as row names.
Default \code{"sample_id"}.}

\item{ref}{Optional named list. Each name is a column to convert to a
factor, and each value the level to use as the reference (first) level,
as in \code{\link[stats:relevel]{stats::relevel()}}. Use it to set a control or wild-type group as
the baseline before a differential analysis.}
}
\value{
A data.frame with one row per sample, row names set to
\code{rownames}, ordered to match \code{samples} when given.
}
\description{
Joins a cohort's sample map and subject table for one assay, and returns
the result as a base data.frame with row names set to a sample id column.
This is the shape \code{colData} (SummarizedExperiment, DESeq2) and similar
analysis objects expect.
}
\examples{
data(example_cohort)
coldata <- as_coldata(example_cohort, assay = "wes")
coldata

as_coldata(example_cohort, assay = "wes", ref = list(genotype = "WT"))

}
\seealso{
\code{\link[=samples]{samples()}}, \code{\link[=join_metadata]{join_metadata()}}
}
