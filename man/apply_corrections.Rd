% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corrections.R
\name{apply_corrections}
\alias{apply_corrections}
\title{Apply documented corrections to a manifest}
\usage{
apply_corrections(manifest, corrections)
}
\arguments{
\item{manifest}{A data frame with one row per sample and at least a
\code{subject_id} and a \code{sample_id} column.}

\item{corrections}{A data frame with columns \code{level}, \code{id}, \code{column},
\code{value}, and \code{reason}. \code{level} is \code{"subject"} or \code{"sample"}. \code{id} names
the subject or sample. \code{column} names the manifest column to change and
\code{value} is the new value. See \code{\link[=read_corrections]{read_corrections()}} to read one from a
file.}
}
\value{
The corrected manifest as a tibble. The \code{"corrections"} attribute
holds the audit table, which \code{\link[=corrections_log]{corrections_log()}} returns.
}
\description{
Applies a corrections table to a long-format manifest, one row per sample,
and records what changed. A study keeps its overrides in one table with a
reason for each, instead of inline edits spread over scripts.
}
\details{
A subject correction changes every manifest row for that subject. A sample
correction changes the row or rows for that sample. Corrections are
applied in order, so a later row can overwrite an earlier one for the same
cell. A corrected column becomes character, and an \code{NA} value clears the
cell.

The audit table has one row per correction: \code{level}, \code{id}, \code{column},
\code{old_value}, \code{new_value}, \code{reason}, and \code{n_rows}, the number of manifest
rows that changed. When the old values differ across those rows they are
joined with \code{"; "}. Applying corrections to an already corrected manifest
appends to the existing audit table.

The function errors when a required column of \code{corrections} is missing,
when a level is not \code{"subject"} or \code{"sample"}, when an id is not in the
manifest, or when a column is not in the manifest.
}
\examples{
manifest <- tibble::tibble(
  subject_id = c("R1", "R1", "R2"),
  assay = c("wes", "wes", "wes"),
  sample_id = c("R1_T", "R1_N", "R2_T"),
  genotype = c("WT", "WT", "KO"),
  fastq = c("r1_t.fq.gz", "r1_n.fq.gz", "r2_t.fq.gz")
)
corrections <- tibble::tibble(
  level = c("subject", "sample"),
  id = c("R1", "R2_T"),
  column = c("genotype", "fastq"),
  value = c("KO", "r2_tumor.fq.gz"),
  reason = c("genotyping rerun on 2026-03-01", "vendor renamed the file")
)

corrected <- apply_corrections(manifest, corrections)
corrected
corrections_log(corrected)
}
\seealso{
\code{\link[=read_corrections]{read_corrections()}}, \code{\link[=corrections_log]{corrections_log()}}
}
