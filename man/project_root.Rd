% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{project_root}
\alias{project_root}
\title{Find the project root folder}
\usage{
project_root(start = ".", markers = c(".git", "DESCRIPTION", ".Rproj"))
}
\arguments{
\item{start}{Folder to start from. Defaults to the working directory.}

\item{markers}{Character vector of names to look for. A marker matches an
entry with the same name. A marker that starts with a dot, such as
\code{.Rproj}, also matches any entry that ends with it.}
}
\value{
The absolute, normalized path of the first folder that holds a
marker, as an \code{fs_path}.
}
\description{
Walks up from \code{start} until a folder holds one of the \code{markers}. The
default markers are a \code{.git} entry, a \code{DESCRIPTION} file, or any file that
ends in \code{.Rproj}. Scripts can then build paths from the root instead of
from the working directory.
}
\examples{
root <- fs::path(tempfile(), "study")
fs::dir_create(fs::path(root, "scripts", "qc"))
fs::file_create(fs::path(root, "DESCRIPTION"))

project_root(fs::path(root, "scripts", "qc"))

unlink(fs::path_dir(root), recursive = TRUE)
}
\seealso{
\code{\link[=project_path]{project_path()}} to join paths under the root.
}
