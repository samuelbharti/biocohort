% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{load_analysis}
\alias{load_analysis}
\title{Load an analysis's feature table from disk}
\usage{
load_analysis(cohort, spec, reader = NULL)
}
\arguments{
\item{cohort}{A \link{Cohort} providing \code{paths} (for \code{{root}}), subjects, and the
sample map (for pair-level enumeration).}

\item{spec}{An \link{AnalysisSpec} or the name of one registered in \code{cohort}.}

\item{reader}{Optional reader override: a function, or a \code{"fun"}/\code{"pkg::fun"}
name. Defaults to the spec's \code{reader}.}
}
\value{
A list with:
\itemize{
\item \code{data}: a tibble of all loaded rows (empty if no files were found),
with provenance key columns added.
\item \code{files}: a tibble with one row per unit: its keys, the resolved \code{path},
and whether it \code{exists}.
}
}
\description{
Resolves an \link{AnalysisSpec}'s \code{path_template} for each unit implied by its
\code{level} (one file per subject, per pair, or one for the whole cohort), reads
the existing files with the spec's \code{reader}, and row-binds them into a single
feature table annotated with provenance keys (\code{subject_id} and/or \code{pair_id}).
}
\details{
Path tokens supported: \code{{root}} (from \code{cohort@paths[[root_key]]}),
\code{{subject_id}}, and for pair-level specs \code{{tumor_sample_id}},
\code{{normal_sample_id}}, \code{{pair_id}} (derived via \code{\link[=sample_pairs]{sample_pairs()}}). Missing
files are skipped (with a warning) and recorded in \code{files}, so loading is
never silently partial.
}
\examples{
# Write a per-subject CSV, then load it.
dir <- tempfile()
dir.create(dir)
write.csv(
  data.frame(gene = "TP53", value = 1), file.path(dir, "S1.csv"),
  row.names = FALSE
)

manifest <- data.frame(
  subject_id = "S1", species = "human", assay = "rna", sample_id = "x"
)
parsed <- validate_manifest(manifest)
cohort <- cohort_new(
  subject_tbl = parsed$subject_tbl, sample_map = parsed$sample_map,
  paths = list(rna_root = dir)
)
spec <- analysis_spec_new(
  name = "expr", assay = "rna", level = "subject", format = "csv",
  path_template = "{root}/{subject_id}.csv", root_key = "rna_root",
  reader = "read.csv", key_cols = "subject_id", feature_type = "gene"
)
loaded <- load_analysis(cohort, spec)
loaded$data
loaded$files

}
\seealso{
\code{\link[=load_analyses]{load_analyses()}}, \code{\link[=orthologize]{orthologize()}}
}
