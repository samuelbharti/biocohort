% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_spec.R
\name{AnalysisSpec}
\alias{AnalysisSpec}
\title{S7 AnalysisSpec class}
\usage{
AnalysisSpec(
  name = character(0),
  assay = character(0),
  level = character(0),
  format = character(0),
  description = NA_character_,
  path_template = NA_character_,
  root_key = NA_character_,
  reader = character(0),
  key_cols = character(0)
)
}
\arguments{
\item{name}{Character scalar for unique analysis name (key in registry).}

\item{assay}{Character scalar for assay type (e.g., "wes_somatic",
"wes_germline", "snrna"). Required.}

\item{level}{Character scalar for the granularity at which the analysis
produces results. Must be one of:
\itemize{
\item \code{"subject"}: one result per subject.
\item \code{"pair"}: one result per tumor/normal (case/control) pair, as derived by
\code{\link[=sample_pairs]{sample_pairs()}} from the cohort's \code{sample_map}.
\item \code{"cohort"}: a single result for the whole cohort.
}

Required.}

\item{format}{Character scalar for file format (e.g., "rds", "tsv", "txt").
Required.}

\item{description}{Character scalar for human-readable description of the
analysis. Optional, defaults to NA.}

\item{path_template}{Character scalar for templated path to analysis output.
Supports substitution tokens: \code{{root}} (from \code{root_key}), \code{{subject_id}},
and the pair tokens \code{{tumor_sample_id}}, \code{{normal_sample_id}}, \code{{pair_id}}
(the latter three supplied by \code{\link[=sample_pairs]{sample_pairs()}} for \code{level = "pair"}).
Optional, defaults to NA.}

\item{root_key}{Character scalar for key in cohort@paths list to use as
the \code{{root}} template value (e.g., "msi_root", "sig_root", "wes_root").
Optional, defaults to NA.}

\item{reader}{Character scalar for function name to read files matching
this spec (e.g., "read_msi_txt", "read.csv"). Must be a valid function
name. Required.}

\item{key_cols}{Character vector of column names to use as keys when
loading the analysis table. Determines how rows are indexed
(e.g., \code{c("subject_id")} or \code{c("pair_id")}). Required.}
}
\description{
An immutable S7 class for defining analysis specifications in a Cohort.
AnalysisSpec objects describe how to locate, read, and interpret analysis
output files with templated paths and standardized readers.
}
\details{
Use \code{\link[=analysis_spec_new]{analysis_spec_new()}} to construct AnalysisSpec objects with immediate
validation. AnalysisSpec objects are typically registered in a Cohort via
\code{\link[=analysis_register]{analysis_register()}}.

Access properties via the \code{@} operator:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{spec@name
spec@assay
spec@level
spec@format
spec@description
spec@path_template
spec@root_key
spec@reader
spec@key_cols
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=analysis_spec_new]{analysis_spec_new()}} for object construction,
\code{\link[=analysis_register]{analysis_register()}} for registering specs in a Cohort
}
