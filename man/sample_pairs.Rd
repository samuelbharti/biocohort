% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairs.R
\name{sample_pairs}
\alias{sample_pairs}
\title{Derive sample pairs from a sample map}
\usage{
sample_pairs(
  sample_map,
  assays = NULL,
  tumor_role = "tumor",
  normal_role = "normal"
)
}
\arguments{
\item{sample_map}{A long-format sample map (e.g. from \code{\link[=validate_manifest]{validate_manifest()}}),
with columns \code{subject_id}, \code{assay}, \code{sample_id}, and \code{role}. A Cohort's
\code{sample_map} (i.e. \code{cohort@sample_map}) can be passed directly.}

\item{assays}{Optional character vector of assays to restrict pairing to.
\code{NULL} (default) pairs within every assay present in \code{sample_map}.}

\item{tumor_role}{Character scalar naming the role treated as the tumor (or
"case") side of a pair. Default \code{"tumor"}.}

\item{normal_role}{Character scalar naming the role treated as the normal (or
"control") side of a pair. Default \code{"normal"}.}
}
\value{
A tibble with one row per derived pair and columns:
\itemize{
\item \code{subject_id}: subject the pair belongs to.
\item \code{assay}: assay the pair was derived within.
\item \code{tumor_sample_id}: sample id of the \code{tumor_role} member.
\item \code{normal_sample_id}: sample id of the \code{normal_role} member.
\item \code{pair_id}: composite id, \code{paste0(tumor_sample_id, "__", normal_sample_id)}.
}

Subjects lacking either role within an assay contribute no rows. When a
subject has multiple tumor and/or normal samples in an assay, all
tumor x normal combinations are enumerated.
}
\description{
Builds tumor/normal (or, more generally, paired) sample combinations from a
canonical long-format \code{sample_map}. Pairing is assay-agnostic: any assay that
records two roles (e.g. WGS, WES) can be paired with the same helper. Within
each \code{subject_id} x \code{assay}, every sample with \code{tumor_role} is paired with
every sample carrying \code{normal_role}.
}
\examples{
manifest <- data.frame(
  subject_id = c("S1", "S1", "S1", "S2"),
  species = c("rat", "rat", "rat", "rat"),
  assay = c("wes", "wes", "scrna", "wes"),
  sample_id = c("T1", "N1", "R1", "N2"),
  role = c("tumor", "normal", "tumor", "normal"),
  stringsAsFactors = FALSE
)
parsed <- validate_manifest(manifest)

# S1 has a WES tumor/normal pair; S2 has only a normal, so it is dropped.
sample_pairs(parsed$sample_map)

# Restrict to specific assays
sample_pairs(parsed$sample_map, assays = "wes")

}
\seealso{
\code{\link[=validate_manifest]{validate_manifest()}} for producing a \code{sample_map}
}
