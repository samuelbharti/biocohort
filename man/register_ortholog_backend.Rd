% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orthologs.R
\name{register_ortholog_backend}
\alias{register_ortholog_backend}
\title{Register a gene-ortholog backend}
\usage{
register_ortholog_backend(name, fn)
}
\arguments{
\item{name}{Character scalar naming the backend.}

\item{fn}{A function with signature
\verb{function(features, from, to, gene_col, id_type, ...)} returning a list
with two tibbles:
\itemize{
\item \code{mapped}: input rows that had at least one ortholog, carrying the
\code{.ortholog_id} key and an \code{ortholog} column with the target-species id.
\item \code{unmapped}: input rows (carrying \code{.ortholog_id}) with no ortholog.
}}
}
\value{
Invisibly, the backend name.
}
\description{
Adds a named ortholog backend so it can be selected by name in
\code{\link[=ortholog_genes]{ortholog_genes()}}. A backend performs gene-level cross-species mapping; this
pluggable design mirrors the liftover backends used for coordinate features.
}
\seealso{
\code{\link[=ortholog_backends]{ortholog_backends()}}, \code{\link[=ortholog_genes]{ortholog_genes()}}, \code{\link[=ortholog_babelgene]{ortholog_babelgene()}}
}
