% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liftover.R
\name{liftover_intervals}
\alias{liftover_intervals}
\title{Liftover a set of genomic intervals across assemblies or species}
\usage{
liftover_intervals(
  intervals,
  chain,
  from = NA_character_,
  to = NA_character_,
  backend = "rtracklayer",
  ...
)
}
\arguments{
\item{intervals}{A data.frame or tibble of intervals. Required columns:
\code{seqnames} (character), \code{start} (integer), \code{end} (integer). Optional
\code{strand}; any additional columns are preserved on \code{unmapped} rows.}

\item{chain}{Path to a chain file (e.g. a UCSC \code{.chain}/\code{.chain.gz}), or a
backend-specific chain object. Cross-species chains (e.g. rat-to-human)
enable cross-species liftover where synteny permits.}

\item{from, to}{Optional character scalars recording the source and target
assembly/species for provenance.}

\item{backend}{Either the name of a registered backend (see
\code{\link[=liftover_backends]{liftover_backends()}}) or a backend function. Defaults to \code{"rtracklayer"}.}

\item{...}{Additional arguments passed to the backend.}
}
\value{
A \link{TranslationResult}.
}
\description{
Translates coordinate features (intervals such as variants, peaks, or regions)
from one genome assembly or species to another using a chain file, via a
pluggable backend. Returns a \link{TranslationResult} that keeps both the mapped
and the unmapped features, so loss is explicit.
}
\details{
Cross-species liftover is inherently lossy and limited to syntenic, alignable
regions; non-conserved regions (and many regulatory elements) will not map.
Always inspect \code{\link[=translation_stats]{translation_stats()}} and the \code{unmapped} table rather than
assuming full recovery. For allele-aware variant (VCF) translation, see
\code{\link[=liftover_vcf]{liftover_vcf()}}.
}
\examples{
ints <- data.frame(
  seqnames = c("chr1", "chr1"),
  start = c(100, 5000),
  end = c(200, 5100)
)
# Illustrative in-memory backend (maps the first interval, drops the rest):
backend <- function(intervals, chain, ...) {
  list(
    mapped = tibble::tibble(
      .liftover_id = intervals$.liftover_id[1],
      seqnames = "chrT", start = 1L, end = 100L, strand = "*"
    ),
    unmapped = intervals[-1, , drop = FALSE]
  )
}
res <- liftover_intervals(ints, chain = "none", to = "human", backend = backend)
translation_stats(res)

}
\seealso{
\code{\link[=liftover_rtracklayer]{liftover_rtracklayer()}}, \code{\link[=liftover_crossmap]{liftover_crossmap()}}, \code{\link[=orthologize]{orthologize()}}
}
