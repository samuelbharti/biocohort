% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_registry.R
\name{analysis_list}
\alias{analysis_list}
\title{List registered analysis specifications}
\usage{
analysis_list(cohort)
}
\arguments{
\item{cohort}{A Cohort object.}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{name}: Analysis name (chr)
\item \code{assay}: Assay type (chr)
\item \code{level}: Data level - "subject", "pair", or "cohort" (chr)
\item \code{format}: File format (chr)
\item \code{reader}: Reader function name (chr)
\item \code{root_key}: Optional root path key (chr)
}

If the cohort has no registered specs, returns an empty tibble with
these columns.
}
\description{
Returns a tibble with one row per registered AnalysisSpec in a Cohort.
Summarizes key metadata for quick inspection of available analyses.
}
\details{
The returned tibble includes only the most essential metadata fields
for discovery and filtering. Use \code{\link[=analysis_spec]{analysis_spec()}} to retrieve the
full AnalysisSpec object including description, path_template, and
key_cols.
}
\examples{
# Create and register specs
study <- study_new(study_id = "STUDY001", title = "My Study")
manifest <- data.frame(
  subject_id = c("S1", "S1", "S2", "S2"),
  species = c("rat", "rat", "rat", "rat"),
  assay = c("wes", "wes", "wes", "wes"),
  sample_id = c("WES_T1", "WES_N1", "WES_T2", "WES_N2"),
  role = c("tumor", "normal", "tumor", "normal")
)
parsed <- validate_manifest(manifest)
cohort <- cohort_new(
  subject_tbl = parsed$subject_tbl,
  sample_map = parsed$sample_map,
  study = study
)

spec1 <- analysis_spec_new(
  name = "somatic_vars",
  assay = "wes_somatic",
  level = "pair",
  format = "tsv",
  reader = "read_tsv",
  key_cols = c("pair_id")
)

spec2 <- analysis_spec_new(
  name = "gene_expr",
  assay = "snrna",
  level = "subject",
  format = "rds",
  reader = "readRDS",
  key_cols = c("subject_id")
)

cohort <- analysis_register(cohort, spec1)
cohort <- analysis_register(cohort, spec2)
analysis_list(cohort)

}
\seealso{
\code{\link[=analysis_register]{analysis_register()}} for registering specs,
\code{\link[=analysis_spec]{analysis_spec()}} for retrieving a full spec object
}
