% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liftover.R
\name{liftover_vcf}
\alias{liftover_vcf}
\title{Liftover a VCF of variants with CrossMap (allele-aware)}
\usage{
liftover_vcf(
  vcf,
  chain,
  ref_fasta,
  out = tempfile(fileext = ".vcf"),
  from = NA_character_,
  to = NA_character_,
  crossmap = NULL
)
}
\arguments{
\item{vcf}{Path to the input VCF.}

\item{chain}{Path to a chain file.}

\item{ref_fasta}{Path to the target-genome FASTA.}

\item{out}{Output VCF path. Defaults to a temporary file.}

\item{from, to}{Optional provenance strings.}

\item{crossmap}{Path or name of the CrossMap executable. Defaults to
auto-detection on the \code{PATH}.}
}
\value{
A \link{TranslationResult} whose \code{mapped} and \code{unmapped} carry the output
and unmapped VCF paths; \code{stats} records the number of unmapped records.
}
\description{
Thin, experimental wrapper around \verb{CrossMap vcf}, which performs allele-aware
variant liftover (updating the REF allele against the target genome and
handling strand flips) -- something plain interval liftover does not do.
Requires CrossMap on the \code{PATH} and a target-genome FASTA.
}
\details{
This function is experimental and intentionally minimal: it runs CrossMap and
reports the produced paths and unmapped count rather than parsing variants
into R. Parse the output VCF with your tool of choice (e.g.
\code{VariantAnnotation}).
}
\seealso{
\code{\link[=liftover_intervals]{liftover_intervals()}}, \code{\link[=liftover_crossmap]{liftover_crossmap()}}
}
