% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_yaml.R
\name{read_study_yaml}
\alias{read_study_yaml}
\title{Build a cohort from a study YAML file}
\usage{
read_study_yaml(path, strict = TRUE)
}
\arguments{
\item{path}{Path to the study YAML file.}

\item{strict}{Logical. When \code{TRUE} (default), an unknown top-level key
is an error. Set \code{FALSE} to read a \verb{study:} block out of a larger
configuration file that has other keys of its own.}
}
\value{
A \link{Cohort} built from the file.
}
\description{
Reads a study, its manifest, its file paths, and its registered analyses
from one YAML file, and returns a ready-to-use \link{Cohort}. This turns the
handful of calls a study's setup script usually makes (\code{study_new()},
\code{read_manifest()}, \code{cohort_new()}, one \code{analysis_spec_new()} per analysis)
into one function call and one file to edit.
}
\details{
The file has these top-level keys, all optional except \code{manifest}:
\itemize{
\item \code{study}: fields for \code{\link[=study_new]{study_new()}} (\code{study_id}, \code{title}, \code{description},
\code{hypotheses}, \code{aims}, \code{assays}, \code{genome_builds}, \code{tags}).
\item \code{manifest}: path to the manifest file, read with \code{\link[=read_manifest]{read_manifest()}}.
Required.
\item \code{sample_cols}: extra sample-level columns, passed to
\code{\link[=validate_manifest]{validate_manifest()}}.
\item \code{species}: fills a \code{species} column when the manifest has none.
\item \code{paths}: a map of root name to path, stored as \code{cohort@paths}.
\item \code{corrections}: path to a corrections file, applied to the manifest with
\code{\link[=apply_corrections]{apply_corrections()}} before it is validated.
\item \code{analyses}: a list of \code{\link[=analysis_spec_new]{analysis_spec_new()}} field sets, one per
registered analysis.
}

Every path (\code{manifest}, an entry of \code{paths}, \code{corrections}) is resolved
relative to the YAML file's own directory unless it is already absolute.
}
\examples{
dir <- tempfile()
dir.create(dir)
writeLines(
  c(
    "subject_id,species,assay,sample_id,role",
    "R1,rat,wes,T1,tumor",
    "R1,rat,wes,N1,normal"
  ),
  file.path(dir, "manifest.csv")
)
writeLines(
  c(
    "study:",
    "  study_id: PILOT",
    "  title: Example pilot",
    "manifest: manifest.csv"
  ),
  file.path(dir, "study.yaml")
)

cohort <- read_study_yaml(file.path(dir, "study.yaml"))
cohort

}
\seealso{
\code{\link[=write_study_yaml]{write_study_yaml()}}, \code{\link[=read_manifest]{read_manifest()}}, \code{\link[=cohort_new]{cohort_new()}}
}
