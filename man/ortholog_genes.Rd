% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orthologs.R
\name{ortholog_genes}
\alias{ortholog_genes}
\title{Map gene-level features to orthologs in another species}
\usage{
ortholog_genes(
  features,
  from,
  to,
  gene_col = "gene",
  id_type = c("symbol", "entrez", "ensembl"),
  backend = "babelgene",
  ...
)
}
\arguments{
\item{features}{A data.frame or tibble with one column of gene identifiers
(\code{gene_col}). Other columns (e.g. expression values) are preserved on the
\code{mapped} rows alongside the new \code{ortholog} column.}

\item{from, to}{Character scalars naming the source and target species (e.g.
\code{"human"}, \code{"mouse"}, \code{"rat"}). Both required.}

\item{gene_col}{Name of the column in \code{features} holding gene identifiers.
Defaults to \code{"gene"}.}

\item{id_type}{Identifier type: one of \code{"symbol"}, \code{"entrez"}, \code{"ensembl"}.}

\item{backend}{Either the name of a registered backend (see
\code{\link[=ortholog_backends]{ortholog_backends()}}) or a backend function. Defaults to \code{"babelgene"}.}

\item{...}{Additional arguments passed to the backend.}
}
\value{
A \link{TranslationResult}. \code{mapped} contains the input columns plus an
\code{ortholog} column with target-species identifiers (one input gene may yield
multiple ortholog rows).
}
\description{
Translates gene-level features (a table with a gene-identifier column) from
one species to another via a pluggable ortholog backend. Returns a
\link{TranslationResult} retaining both mapped and unmapped rows, so loss is
explicit.
}
\details{
Ortholog mapping is many-to-many in general: a gene may have zero, one, or
several orthologs. Inspect \code{\link[=translation_stats]{translation_stats()}} and the \code{unmapped} table
rather than assuming one-to-one correspondence.
}
\examples{
features <- data.frame(
  gene = c("TP53", "MYC", "NOT_A_GENE"),
  expr = c(1.2, 3.4, 5.6)
)
# Illustrative in-memory backend (uppercases to a fake "ortholog"):
backend <- function(features, from, to, gene_col, id_type, ...) {
  ok <- features[[gene_col]] != "NOT_A_GENE"
  mapped <- features[ok, , drop = FALSE]
  mapped$ortholog <- tolower(mapped[[gene_col]])
  list(mapped = mapped, unmapped = features[!ok, , drop = FALSE])
}
ortholog_genes(features, from = "human", to = "mouse", backend = backend)

}
\seealso{
\code{\link[=orthologize]{orthologize()}}, \code{\link[=ortholog_babelgene]{ortholog_babelgene()}}, \link{TranslationResult}
}
