% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{Cohort}
\alias{Cohort}
\title{S7 Cohort class}
\usage{
Cohort(
  study = NULL,
  subject_tbl = tibble::tibble(subject_id = character(), species = character()),
  sample_map = tibble::tibble(subject_id = character(), assay = character(), sample_id =
    character(), role = character()),
  paths = list(),
  analyses = list(),
  registry = list(),
  cache = list()
)
}
\arguments{
\item{study}{A Study object with project-level context, or NULL.}

\item{subject_tbl}{A data frame with one row per subject. Required columns:
\code{subject_id} and \code{species}, both character. Common optional columns:
\code{sex}, \code{strain}, \code{genotype}, \code{cohort}, \code{timepoint}, \code{notes}. Checked by
\code{\link[=validate_cohort]{validate_cohort()}}. Defaults to an empty table with the two required
columns.}

\item{sample_map}{A long-format data frame with one row per sample.
Required columns: \code{subject_id}, \code{assay}, \code{sample_id}, \code{role}, all
character. A new assay is a new row, never a new column. Checked by
\code{\link[=validate_cohort]{validate_cohort()}}. Defaults to an empty table with the four required
columns.}

\item{paths}{Named list of file paths to data files or result folders.
Defaults to an empty list.}

\item{analyses}{Named list of analysis tables or other data objects.
Defaults to an empty list.}

\item{registry}{Named list of AnalysisSpec objects. Names match
\code{spec@name}. Defaults to an empty list.}

\item{cache}{Named list used to memoize loaded analysis data. Defaults to
an empty list.}
}
\description{
An S7 class that keeps the subjects and samples of a study in one object.
A Cohort holds a subject table, a long-format sample map, an optional
Study, file paths, analysis tables, and a registry of analysis specs.
}
\details{
Use \code{\link[=cohort_new]{cohort_new()}} to build a Cohort. It checks the input types, converts
both tables to tibbles, and runs \code{\link[=validate_cohort]{validate_cohort()}}. Construction itself
also checks \code{subject_tbl} and \code{sample_map} with the same rules, so building
a \code{Cohort} any other way still enforces the required columns.

Subjects live only in \code{subject_tbl}. Use \code{\link[=subject]{subject()}} to read one row as a
\link{Subject} object.

Access properties with the \code{@} operator:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cohort@study         # Study object or NULL
cohort@subject_tbl   # Subject metadata table
cohort@sample_map    # Sample mapping table
cohort@paths         # File paths
cohort@analyses      # Stored analysis results
cohort@registry      # Named list of AnalysisSpec objects
cohort@cache         # Memoization cache
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=cohort_new]{cohort_new()}} for object construction,
\code{\link[=subject]{subject()}} for reading one subject,
\code{\link[=validate_cohort]{validate_cohort()}} for validation details,
\code{\link[=validate_manifest]{validate_manifest()}} for manifest preparation,
\code{\link[=read_manifest_csv]{read_manifest_csv()}} for loading manifest from file,
\code{\link[=analysis_register]{analysis_register()}} for registering analyses
}
